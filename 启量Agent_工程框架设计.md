# 启量 Agent · 工程框架设计文档

> 版本：v3.0  
> 日期：2026-06-16  
> 状态：六阶段推演闭环，MVP 边界锁定，PRD 验收标准就绪  
> 核心理念：业务 > 产品力 > 供应链 > AI 工具  
>
> 推演记录：
> - [P1 商业战略](冲刺/启量Agent战略推演_第一阶段总结.txt) (5/25)
> - [P2 技术架构](冲刺/启量Agent战略推演_第一阶段总结.txt) (5/27)
> - [P3 安全合规](冲刺/启量Agent战略推演_第三阶段总结.txt) (6/7)
> - [P3' 利益相关者](冲刺/启量Agent战略推演_第三阶段_利益相关者与价值流转.txt) (6/8)
> - [P4 核心工作流](冲刺/启量Agent战略推演_第四阶段_核心工作流与价值流转.txt) (6/9)
> - [P5 MVP 边界裁剪](冲刺/启量Agent战略推演_第五阶段_MVP边界与功能裁剪.txt) (6/10~11)
> - [P6 PRD 验收标准](冲刺/启量Agent战略推演_第六阶段_PRD验收标准定义.txt) (6/16)

---

## 一、项目定位

| 维度 | 定义 |
|------|------|
| **商业模式** | TikTok 矩阵自营 — 自有产品 + 自购账号 + 自产内容 |
| **品类** | 卖书（标品、无损耗、每本书自带 20+ 内容角度） |
| **规模** | 生产环境：15 人团队 / 45 个号 / 日产 405 条 |
| | MVP：3 本书 / 5 个测试号 / 日产 10-15 条 |
| **核心壁垒** | 人工采集 × 深度理解 = 护城河。AI 只是增效工具 |
| **战略路线** | 平台合规（A）+ 垂直品类深耕（B） |
| **不可能三角** | 深度理解 × 规模化产出 × 低成本 → 牺牲运营效率 |

---

## 二、MVP 版本分层总览（V1.0 vs V2.0）

> P5 产出：九刀砍下去，V1.0 只保留验证核心假设的最小骨架。

### 2.1 核心假设

```
深度理解（人读一本书 → 投喂「讲什么」库）
  + 表达模板（人刷 TikTok → 投喂「怎么讲」库）
  → LLM 生成脚本 → 能产出有完播率、有互动、有转化的内容
```

### 2.2 V1.0 规模

- 3 本书（不同品类，覆盖面够宽）
- 5 个测试号
- 日产 10-15 条
- 周期：跑满 3 周出数据

### 2.3 砍掉/保留/延后 裁决表

| 功能 | V1.0 状态 | V1.0 替代方案 | 复活触发条件 |
|------|----------|-------------|------------|
| Phase 4 全自动装配（BGM/关键帧/调色/剪映JSON） | ✗ 砍 | 人工剪映，5 分钟/条 | 日产 > 50 条 |
| Phase 4 时间轴对齐 + 字幕生成 | ✓ 保留 | — | — |
| 违禁词 RAG 五层自动闭环 | ✗ 砍 | 静态 Excel + 人工全量扫 | 账号 > 20 AND 安全事件 > 3 次 |
| changedetection.io + LLM 拆解 | ✓ 保留 | — | — |
| 加密硬阀门系统（三层加密 + 死手开关） | ✗ 砍 | Git PR review SOP | 团队 > 3 AND 日产 > 50 AND 老板施压 |
| Embedding 语义指纹去重 | ✗ 延 | 人眼 60 秒扫 15 条 | 日产 ≥ 50 条 |
| TikTok API 自动数据采集 + 评论聚类 | ✗ 砍 | 人工刷后台 + 人眼看评论区 | 账号 > 10 / 日均评论 > 50 |
| FastAPI REST 层 | ✗ 延 | Streamlit 直连 ORM | 多服务通信需求出现 |
| 「怎么讲」库自动计数 + 自动废弃 | ✗ 移 V2 | 手动 tally（每天 60 秒） | 表达模板 > 50 条 |
| Phase 3 视频生成（MiniMax video-01） | ✓ 保留 | — | — |
| Phase 2 asyncio 并发 | ✓ 保留 | — | — |
| 双 RAG 库 + 上下游联动 | ✓ 保留 | — | — |
| Phase 1-3 核心管线 | ✓ 保留 | — | — |
| MySQL + Tortoise ORM | ✓ 保留 | — | — |

### 2.4 V1.0 验证成功/失败信号

**成功：**
- 某条视频完播率 > 30% 且平均观看 > 15 秒
- 某条视频日均涨粉从个位数跳到 10+
- 有一条自然评论不是"广告矩阵别上当"

**失败：**
- 3 周后 5 个号全无起色
- 所有视频完播率 < 15%
- 评论区出现"广告矩阵别上当"

---

## 三、技术架构总览（v3.0 双 RAG 库 + MVP 安全层）

```
                        ┌─────────────────────────────────────┐
                        │        Layer 0：安全合规天幕          │
                        │  changedetection → LLM拆解 → PM决策  │
                        │  V1.0 静态违禁词 Excel + 人工全量审核  │
                        │  安全扫描 AC-SEC-01 ~ 06             │
                        └──────────────┬──────────────────────┘
                                       │ 规则变更驱动
                                       ▼
┌──────────────────────────────────────────────────────────────────┐
│                    采集层（人工 · 双入口 · V1.0 Git PR SOP 管控）    │
│                                                                  │
│  ┌─────────────────────┐              ┌─────────────────────┐    │
│  │ 「讲什么」深度理解采集 │              │ 「怎么讲」表达模式采集 │    │
│  │ · 人读一本书          │              │ · 人刷 TikTok       │    │
│  │ · 投喂个人深度理解     │              │ · 填入爆款脚本       │    │
│  │ · V1.0: Git PR review│              │ · V1.0: Git PR review│   │
│  │ · V2.0: 加密硬阀门     │              │ · V2.0: 加密硬阀门    │    │
│  └──────────┬──────────┘              └──────────┬──────────┘    │
│             │                                    │                │
│             ▼                                    ▼                │
│  ┌─────────────────────┐              ┌─────────────────────┐    │
│  │ 「讲什么」RAG 库      │              │ 「怎么讲」RAG 库      │    │
│  │ · 内容实质 / 静态资产  │   上下游     │ · 表达形式 / 消耗品    │    │
│  │ · 每书一次/永久复用    │   联动！     │ · V1.0: 手动 tally    │    │
│  │                      │◄───────────▶│ · V2.0: 自动计数      │    │
│  └──────────┬──────────┘              └──────────┬──────────┘    │
│             │                                    │                │
│             └──────────────┬─────────────────────┘                │
│                            │ 两股输入流在 Prompt 中交汇             │
│                            ▼                                      │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │                    四阶段自动化流水线                       │    │
│  │                                                          │    │
│  │  ┌──────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────┐ │
│  │  │ Phase 1  │──▶│   Phase 2    │──▶│   Phase 3    │──▶│ Phase 4  │ │
│  │  │ 脚本精炼  │   │ 翻译+分镜并发 │   │ 多模态路由    │   │ 精简版    │ │
│  │  │ 双锚点   │   │ 安全扫描      │   │ TTS+视觉铸造  │   │ 时间轴对齐│ │
│  │  │ Prompt   │   │ AC-SEC 01~06│   │ 视频生成保留  │   │ +字幕生成 │ │
│  │  └──────────┘   └──────────────┘   └──────────────┘   └──────────┘ │
│  │                                                          │      │
│  │  V1.0: BGM/关键帧 → 人工剪映                              │      │
│  │  V2.0: 全自动装配车间复活                                   │      │
│  └──────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘

调度规则（硬依赖链）：
  上游为空 → 整条管线静默
  下游耗尽 → 管线降级，等待人工采集
  两库齐备 → 全速运转
```

---

## 四、四阶段流水线详设

### Phase 1：脚本精炼引擎

| 项目 | 规格 |
|------|------|
| **输入** | 用户原始短视频剧本（中文） + 「讲什么」深度理解锚点 + 「怎么讲」表达模板 |
| **处理** | 正则预处理 → DeepSeek V3（temperature=0.1, JSON mode） |
| **输出** | `hook_sentences[]` / `visual_constraints[]` / `product_pitch[]` |
| **提示词** | `system_prompt.txt` 物理隔离，运营可直接调优 |
| **兜底机制** | JSON 解析失败 → 正则提取 → 空数组兜底 |
| **v3.0** | Prompt 融合双 RAG 锚点 + 违规重生成时注入运营负面约束 |

### Phase 2：翻译 + 分镜异步并发 + 安全扫描

| 项目 | 规格 |
|------|------|
| **并发引擎** | `asyncio.gather` — 翻译通道和分镜通道并行，流式输出实时盯盘 |
| **翻译通道** | DeepSeek V3（temperature=0.3）→ `translated_script` + `translated_pitches` + `localization_notes` |
| **安全扫描** | 翻译通道输出点 → AC-SEC-01~06 验收标准（详见第七章） |
| **分镜通道** | DeepSeek V3（temperature=0.3）→ `storyboard_prompts[]` + `global_style_note` |
| **容错** | `return_exceptions=True` |
| **同步包装** | `asyncio.run()` 供 Streamlit 调用 |

### Phase 3：多模态路由品控

| 模块 | 引擎 | 规格 |
|------|------|------|
| **TTS 配音** | 硅基流动 CosyVoice2-0.5B | 逐句生成 .mp3，失败降级为占位文件 |
| **视觉分发路由** | 黄金 30 秒策略 | ≤30s → 视频 API / >30s → 图像 API |
| **视频生成** | 海螺AI MiniMax video-01 | 异步提交 + 轮询（180s 超时 → 降级为图像） |
| **图像生成** | 海螺AI MiniMax image-01 | 9:16 竖屏，1080×1920 |
| **人机品控** | Streamlit UI | 逐条审核视觉素材 |

### Phase 4：装配车间（V1.0 精简版）

| 模块 | V1.0 状态 | V2.0 复活 |
|------|----------|----------|
| **时间轴对齐** | ✓ 机器做（毫秒级对齐 + 字幕生成） | — |
| **情绪检测** | ✓ 规则引擎 | — |
| **BGM 推荐** | ✗ 人工剪映选取 | 日产 > 50 复活 |
| **转场方案** | ✗ 人工剪映操作 | 日产 > 50 复活 |
| **关键帧方案** | ✗ 人工剪映操作 | 日产 > 50 复活 |
| **调色预设** | ✗ 人工剪映操作 | 日产 > 50 复活 |
| **最终产出** | 时间轴 XML + 素材包 → 人工剪映装配 | 剪映草稿 JSON 自动导出 |

---

## 五、RAG 知识库设计（v3.0 双库架构）

### 5.1 核心原则

> **质量不是审出来的，是喂进去的。**
>
> 不做实时向量检索，不做整本书语义匹配。  
> 不做生成后幻觉校验。  
> 「讲什么」和「怎么讲」是两个独立的问题 → 两个独立库、两条独立管线。

### 5.2 「讲什么」深度理解库

| 维度 | 定义 |
|------|------|
| **职责** | 回答"这本书的核心是什么"——为 LLM 提供内容实质锚点 |
| **资产类型** | 静态资产，每书只采一次，永久复用 |
| **操作者** | 真正读过该书的人（运营/PM/外聘书评人） |
| **入库管控** | V1.0: Git PR review SOP / V2.0: 加密硬阀门 |

**入库字段规格：**

```json
{
  "book_id": "atomic_habits_001",
  "book_title": "原子习惯",
  "author": "James Clear",
  "deep_understanding": {
    "one_liner": "习惯不是靠意志力坚持的，是靠身份认同投票投出来的",
    "core_insights": [
      {
        "angle": "身份认同 > 目标设定",
        "original_quote": "Every action you take is a vote for the type of person you wish to become",
        "explanation": "不要跟自己说'我要每天跑步'，要说'我是一个跑步的人'",
        "keywords": ["身份", "认同", "投票", "习惯养成"]
      }
    ],
    "unsuitable_angles": [
      "不要讲'每天进步1%一年37倍'——这不是书的核心观点",
      "不要用恐惧驱动"
    ],
    "target_audience": {
      "profile": "想养成习惯但总是失败的人",
      "pain_points": ["三分钟热度", "对自己失望"],
      "desire": "一个真正能持续下去的方法"
    },
    "time_sensitivity": null
  },
  "curated_by": "PM",
  "curated_at": "2026-06-16T10:00:00Z",
  "version": 1
}
```

**关键设计决策：**
- **不适合的角度（负面清单）** 和核心观点同等重要
- 违规重生成 3 次触达上限 → PM 审视该书的 unsuitable_angles 是否需补充
- 与「怎么讲」库物理隔离

### 5.3 「怎么讲」表达模式库

| 维度 | 定义 |
|------|------|
| **职责** | 回答"爆款视频怎么讲"——为 LLM 提供表达锚点 |
| **资产类型** | 消耗品，每条模板使用三次后舍弃 |
| **操作者** | 刷 TikTok 爆款视频的运营人员 |
| **入库管控** | V1.0: Git PR review SOP / V2.0: 加密硬阀门 |
| **计数方式** | V1.0: 手动 tally（Notion 正字计数，每天 60 秒）/ V2.0: 自动计数 |

**入库字段规格：**

```json
{
  "card_id": "script_001",
  "source_url": "tiktok_video_id_xxx",
  "angle_title": "时间管理 × 职场焦虑",
  "template": "你有没有想过，为什么{痛点}？其实背后是{原理}...",
  "rhythm": "快",
  "hook_type": "悬念反转型",
  "emotion": "悬疑",
  "avg_completion": "32%",
  "usage_count": 0,
  "max_usage": 3,
  "deprecated": false,
  "keywords": ["时间管理", "加班", "效率", "职场"]
}
```

### 5.4 管线调度状态机

| 状态 | 「讲什么」库 | 「怎么讲」库 | 管线行为 |
|------|------------|------------|----------|
| 🛑 静默 | 空 | 空/有 | 整条管线静默，不生成任何脚本 |
| ⚠️ 降级 | 有 | 库存 < 3 | 停止批量生成，等待人工采集 |
| ⚠️ 限制 | 有 | 库存 3-10 | 降频生成，提示运营补充 |
| ✅ 全速 | 有 | 库存 ≥ 10 | 全速运转 |

---

## 六、安全合规体系（Layer 0）

### 6.1 架构

```
        Layer 0-A              Layer 0-B              Layer 0-C
    ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
    │  机器自动盯    │ ───▶ │  AI 自动拆解   │ ───▶ │  人类做决策    │
    │              │      │              │      │              │
    │ changedetect │      │ DeepSeek 比对新旧│      │ PM 最终判断   │
    │ 每日截图对比  │      │ 输出：影响面+    │      │ 改什么不改什么 │
    │ Webhook 通知  │      │ 风险等级+建议    │      │              │
    └──────────────┘      └──────────────┘      └──────────────┘
       零代码 10分钟          30秒/次             5分钟/次
```

### 6.2 V1.0 安全扫描流程（精简版）

```
translated_script
  → 静态违禁词 Excel 精确匹配
    → 命中？→ ±15 词可疑邻近词检查
      → 邻近词命中？→ LLM 语义判断（含三段原文证据）
        → 人工最终审核（不论 LLM 结果如何）
          → 安全？→ 放行进入 Phase 3
          → 违规？→ 打回 Phase 1 + 运营负面约束 → 重试上限 3 次
            → 触达上限 → 人工接手优化
  → 每日全量兜底：运营逐条扫（15 条 × 30 秒 = 7.5 分钟）
```

### 6.3 V2.0 安全扫描（生产环境复活）

```
① 信号采集层（shadow ban + 违规标签）
  → ② LLM 推断嫌疑词 + 置信度评分
    → ③ 违禁词 RAG 自动更新（语义向量入库）
      → ④ 语义匹配扫描
        → ⑤ 分发前熔断
  置信度分层：永久封禁级零容忍 / 功能限制级 90% / 内容限流级 70%
```

---

## 七、PRD 验收标准：Phase 2 安全扫描 AC-SEC-01 ~ 06

> P6 产出：六轮 PM 决策 → 六条机器可读的验收标准。工程师可直接翻译为 if-else + 正则 + 重试上限，不需要向 PM 打一个电话。

```
AC-SEC-01：违禁词精确匹配
  输入：Phase 2 翻译通道输出的 translated_script（纯文本，英文）
  校验对象：PM 手动维护的违禁词 Excel（路径：/config/banned_terms.csv）
  匹配方式：大小写不敏感精确匹配 + 词边界匹配
  输出：命中词列表 [{term, position}]
  通过条件：命中词列表为空
  不通过：进入 AC-SEC-02

AC-SEC-02：可疑邻近词检查
  触发条件：AC-SEC-01 命中词列表非空
  扫描范围：每个命中词 position ± 15 个词
  比对目标：PM 手动维护的可疑邻近词清单（路径：/config/risky_proximity.csv）
  输出：可疑邻近词命中列表 [{term, nearby_term, distance}]
  通过条件：可疑邻近词命中列表为空 → 直接放行，不进 LLM
  不通过：进入 AC-SEC-03

AC-SEC-03：LLM 语义判断
  触发条件：AC-SEC-02 可疑邻近词命中列表非空
  输入：translated_script 全文 + AC-SEC-01 命中词列表 + AC-SEC-02 可疑邻近词列表
  LLM 任务：判断原文在上下文中是否构成 TikTok 社区规范违规
  输出格式：{violation: true|false, confidence: 0-100, evidence: [原文引用×3段]}
  通过条件：violation=false（不论 LLM 判什么，都进 AC-SEC-04）

AC-SEC-04：人工最终审核
  触发条件：AC-SEC-03 已执行（无论 LLM 结果如何）
  审核人：运营人员
  审核内容：translated_script 全文 + LLM 判断结果 + LLM 引用的三段原文证据
  审核动作：
    ① 运营判定"安全"→ 放行，脚本进入 Phase 3
    ② 运营判定"违规"→ 进入 AC-SEC-05

AC-SEC-05：违规重生成
  触发条件：运营判定违规
  动作：打回 Phase 1
    - 保留原「讲什么」锚点
    - 换一个「怎么讲」模板（排除本次使用的模板）
    - 在 Prompt 中注入运营手写的负面约束（如"Avoid mentioning religion"）
  重试上限：3 次
  重试计数：per 脚本维度
  触达上限后：
    - 该脚本移交人工手动优化
    - 该书不暂停，继续生成其他角度
    - PM 收到通知：审视「讲什么」库中该书的 unsuitable_angles 是否需补充

AC-SEC-06：全量安全审核（兜底）
  在 AC-SEC-01 至 AC-SEC-05 之外：
    运营每天对当天全部产出的 translated_script 逐条扫一眼
    耗时：15 × 30 秒 = 7.5 分钟（V1.0）
    发现漏网（Excel 未命中但运营判断违规）：
      ① 该词加入违禁词 Excel
      ② 该脚本打回 AC-SEC-05 流程
```

### 验收标准撰写方法论

好的 AC 必须回答六个问题（缺一不可）：

| 问题 | AC-SEC-01 示例 |
|------|---------------|
| ① 校验对象 | `translated_script` 纯文本 |
| ② 校验方式 | 大小写不敏感精确匹配 + 词边界匹配 |
| ③ 通过条件 | 命中词列表为空 |
| ④ 失败动作 | 进入 AC-SEC-02 |
| ⑤ 重试上限 | 不适用（无重试步骤） |
| ⑥ 输入来源 | `/config/banned_terms.csv`（PM 手动维护） |

反例对照：`"翻译结果应符合 TikTok 社区规范，避免违规内容"` → 六个问题一个都没回答，无法写代码。

---

## 八、RAG 入库管控：V1.0 Git PR Review SOP

> P5 产出：加密硬阀门推迟到 V2.0。V1.0 用 Git 权限模型替代，零代码。

### 权限控制

- RAG JSON 文件放在本地 Git 仓库
- PM 独有 push 权限，实习生只能 fork → PR
- 两条库各自独立 JSON 文件

### 校验流程

| 步骤 | 操作 | 执行人 | 耗时 |
|------|------|--------|------|
| ① | 修改 `rag_deep_understanding.json` 或 `rag_expression.json` | 运营/PM | — |
| ② | `git diff` 自检，确认没有手滑 | 修改人 | 30 秒 |
| ③ | 提交 PR，描述变更内容 | 修改人 | 1 分钟 |
| ④ | PM 扫 PR diff，确认：<br>— 无与已有条目语义重复<br>— `usage_count` 未被手动重置 | PM | 2 分钟 |
| ⑤ | PM merge → 入库生效 | PM | 按钮 |

### 升级触发条件（三缺一不可）

团队 > 3 人 **AND** 日产 > 50 条 **AND** 老板开始问"为什么不能一键铺量"

---

## 九、利益相关者约束：加密硬阀门设计（V2.0）

> P3' 产出：门房一体。V2.0 生产环境激活，V1.0 MVP 阶段以 Git PR SOP 替代。

### 三大利益相关者

| 利益方 | 激励结构 | 天然冲动 |
|--------|----------|----------|
| **矩阵运营老板** | 短期现金流 | 一键铺量、模板复用 100% |
| **TikTok 风控系统** | 延迟惩罚 | 语义聚类检测同源 |
| **海外终端用户** | 信任不可逆衰减 | 第三次刷到 → 广告矩阵标签 |

### 为什么软约束全部无效

| 软约束 | 失效原因 |
|--------|----------|
| Dashboard 警告 | 点 × 关掉 |
| PM 口头论证 | 现金流压力压倒理性 |
| 公司规则文档 | "非常时期非常办法" |
| 代码常量 | 新 PM 一次 commit 删除 |
| 辞职威胁 | 赌职业生涯，不是系统自动执行 |

### 硬阀门三层实现（V2.0）

**第一层：物理隔离** — 加密编译，密钥在 PM 脑中，不存任何共享系统  
**第二层：逻辑绑定** — 嵌入 RAG 入库底层，移除阀门 = 管线验签失败 = 系统降级只读  
**第三层：死手开关** — PM 连续 N 天未续期 → 保守模式（人工采集永久锁死）

### PM 必须清醒认知的代价

> 加密黑盒把"门"和"房子"焊死了。PM 肉身 = 系统单点故障。权力与枷锁共担。

---

## 十、内容指纹去重与元数据清洗

### V1.0 去重方案（Embedding 管线延后）

| 步骤 | V1.0 | V2.0 复活条件 |
|------|------|-------------|
| 1 | 运营每天 LLM 生成完后，全部脚本平铺窗口 | — |
| 2 | 60 秒内扫一遍，发现两条意思撞了 → 标黄 | — |
| 3 | 手动选一条，换「怎么讲」模板重新生成 | — |
| 4 | — | Embedding 自检 + 余弦 > 0.85 打回 |
| **激活** | 当前生效 | 日产 ≥ 50 条 |

### 设备指纹硬隔离

- 一台设备一个号，独立 4G/5G 流量卡
- GPS 真实定位，禁用数据中心 VPN
- 每天 3 个固定发布时段，前置 10 分钟"预热脚本"

### 视频元数据清洗

| 操作 | 工具 |
|------|------|
| 轮换编码器参数 | FFmpeg |
| 随机修改创建时间戳 | FFmpeg metadata |
| 去除/替换设备信息 | FFmpeg |

### 账号权重管理状态机

```
[冷启动探测] ──信号达标──▶ [精准创作] ──粉丝积累──▶ [成熟运营]
     每号 30-50 条         人工 10 条深度          稳定日更
     完播>30%+均观>15秒      日均涨粉 50+            高质量
     阶段切换时 ──▶ 批量隐藏/删除低质探测内容 ◀──
```

---

## 十一、技术栈与目录结构

### 技术栈

| 层 | V1.0 | V2.0 |
|------|------|------|
| **前端** | Streamlit 1.58 | — |
| **LLM 编排** | LangChain + DeepSeek V3 | — |
| **异步引擎** | Python asyncio | — |
| **TTS** | 硅基流动 CosyVoice2-0.5B | — |
| **视频生成** | 海螺AI MiniMax video-01 | — |
| **后端** | Tortoise ORM 直连 MySQL | + FastAPI REST 层 |
| **数据库** | MySQL + aiomysql | — |
| **视频处理** | FFmpeg | — |
| **监控** | changedetection.io | — |
| **入库管控** | Git PR review | 加密硬阀门 |
| **安全扫描** | 静态 Excel + AC-SEC 01~06 | 违禁词 RAG 自动闭环 |
| **去重** | 人眼 60 秒扫 | Embedding 管线 |

### 项目目录

```
jak/
├── app.py                      # Streamlit 主应用
├── phase2_processor.py         # 翻译+分镜异步并发 + 安全扫描
├── phase3_multimodal.py        # TTS + 视觉生成 + 分发路由
├── phase4_assembly.py          # 时间轴对齐 + 字幕生成（V1.0 精简版）
├── system_prompt.txt           # Phase 1 系统提示词
├── prompt_translation.txt      # Phase 2 翻译提示词
├── prompt_storyboard.txt       # Phase 2 分镜提示词
├── .env                        # API Key 配置
├── config/
│   ├── banned_terms.csv        # 违禁词 Excel（PM 手搓，V1.0）
│   └── risky_proximity.csv     # 可疑邻近词（PM 手搓，V1.0）
├── rag/
│   ├── rag_deep_understanding.json  # 「讲什么」库
│   └── rag_expression.json          # 「怎么讲」库
├── phase3_output/              # Phase 3 产出
├── phase4_output/              # Phase 4 产出
├── 冲刺/                       # FastAPI + 推演记录
│   ├── models.py
│   ├── settings.py
│   └── *.txt                   # P1~P6 推演记录
└── 启量Agent_工程框架设计.md     # 本文档
```

---

## 十二、待办清单（v3.0 · V1.0 MVP 冲刺）

### P0：V1.0 阻塞上线

- [ ] Layer 0-A：配置 changedetection.io 监控 TikTok 社区规范页
- [ ] Layer 0-B：编写 LLM 合规拆解 Prompt
- [ ] PM 手搓违禁词 Excel（`banned_terms.csv`）+ 可疑邻近词 Excel（`risky_proximity.csv`）
- [ ] 实现 AC-SEC-01：违禁词精确匹配（词边界 + 大小写不敏感）
- [ ] 实现 AC-SEC-02：可疑邻近词检查（±15 词范围）
- [ ] 实现 AC-SEC-03：LLM 语义判断（三段原文证据 JSON 输出）
- [ ] 实现 AC-SEC-04：Streamlit 人工审核卡片 UI
- [ ] 实现 AC-SEC-05：违规重生成逻辑（上限 3 次 → 人工接手）
- [ ] 实现 AC-SEC-06：全量 translated_script 列表页（运营滚轮扫）

### P0.5：RAG 入库管控

- [ ] Git 仓库初始化 + PM push 权限配置
- [ ] PR review SOP 文档化 + 运营培训

### P1：核心管线

- [ ] 「讲什么」库：JSON schema + 样例数据（3 本书）
- [ ] 「怎么讲」库：JSON schema + 样例数据（15-20 条模板）
- [ ] 手动 tally 计数流程（Notion 模板 + 每天 60 秒 SOP）
- [ ] 双库检索配对逻辑（rhythm filter + keywords）
- [ ] 双库调度状态机（静默/降级/限制/全速）
- [ ] Phase 1 Prompt 改造：融合双 RAG 锚点
- [ ] Phase 2 翻译 + 分镜 asyncio 流式输出
- [ ] Phase 3 TTS + 视频生成 + 路由
- [ ] Phase 4 时间轴对齐 + 字幕生成（精简版）
- [ ] Tortoise ORM models 定义（脚本/账号/RAG 模板/安全事件）

### P2：V1.0 运营 SOP

- [ ] 人工剪映装配 SOP（BGM/关键帧/调色，5 分钟/条）
- [ ] 人肉去重 SOP（15 条平铺 60 秒扫）
- [ ] 数据手动记录 SOP（Notion/Sheets 模板）
- [ ] 评论区人工阅读 → 反馈投喂「讲什么」库 SOP

### P3：V2.0 复活（等触发条件达标）

- [ ] 违禁词 RAG 五层自动闭环
- [ ] 加密硬阀门系统（三层 + 死手开关）
- [ ] Embedding 语义去重管线
- [ ] 「怎么讲」自动计数 + deprecated
- [ ] Phase 4 全自动装配
- [ ] FastAPI REST 层
- [ ] TikTok API 数据自动采集
- [ ] 评论聚类算法

---

## 十三、核心结论备忘（v3.0 · 六阶段全部产出）

```
战略层（P1）：
  1. 与平台博弈是死路 → 适应平台 + 内容质量是唯一可持续路径
  2. 数量 × 质量不可兼得 → 数据已证伪"既要又要"
  3. AI 不是用来躲算法 → 是用来帮内容达到算法要的标准
  4. 业务 > 产品力 > 供应链 > AI 工具

技术层（P2）：
  5. 不做实时 RAG 检索 → 离线预处理 + keyword 索引 + metadata filter
  6. 不做生成后幻觉校验 → 成本炸弹，毛利率撑不住
  7. 做脚本话术 RAG + 图片风格 RAG → 存已验证的表达模式
  8. 对齐在搭建时解决 → metadata rhythm 标签 → 检索时 filter

安全层（P3）：
  9. 安全合规是 Layer 0，罩在所有 Phase 之上的天幕
  10. 违禁词 RAG 闭环（V2.0）：信号采集 → LLM推断 → 自动更新 → 熔断
  11. 语义匹配（与 TikTok 审核同频），精确词库挡不住语境性违规
  12. 置信度分层拦截（V2.0）：永久封禁级零容忍 / 功能限制级 90% / 内容限流级 70%

利益相关者层（P3'）：
  13. 软约束全部无效 → V2.0 加密黑盒 + 死手开关 + 知情同意书 = 门房一体
  14. 拆门 = 拆房子：移除阀门 = 管线验签失败 = 系统降级为只读模式
  15. PM 肉身是唯一单点故障——权力与枷锁共担
  16. 人工采集的高成本 = 护城河的宽度

工作流层（P4）：
  17. 质量不是审出来的，是喂进去的 → 质量闸门建在输入端而非输出端
  18. 五个"优质"标准互相撕咬 → 唯一可牺牲的是运营效率
  19. 双 RAG 库：「讲什么」（深度理解，静态）+「怎么讲」（表达模式，消耗品）
  20. 上下游联动：「讲什么」先有才有「怎么讲」→ 上游为空 = 管线静默
  21. 下游耗尽 = 管线降级 → 物理约束，老板不可绕过
  22. 两股输入流仅在 LLM Prompt 中交汇，各自独立入库、独立加密

MVP 裁剪层（P5）：
  23. MVP 唯一目标 = 验证"深度理解投喂 LLM 能否产出有转化的内容"
  24. 九刀砍下去：Phase 4 全自动砍 / 安全闭环砍 / 加密阀门砍 / 去重延 / 数据采集砍
  25. V1.0 骨架：双 RAG 库 + Phase 1-3 + 时间轴对齐 + 静态安全审核 + MySQL
  26. 每个被砍功能都有白纸黑字的复活触发条件（具体数字，不是"以后"）

PRD 验收标准层（P6）：
  27. AC-SEC-01~06：6 条机器可读的验收标准，工程师可直接翻译为 if-else
  28. 好的 AC 必须回答：校验对象/校验方式/通过条件/失败动作/重试上限/输入来源
  29. 模糊 PRD → AI 幻觉代码。结构化 AC → 零沟通成本。
  30. PM 不需要懂技术实现，但必须能做风险 vs 成本的取舍决策
```

---

*本文档为启量 Agent 工程框架总纲，基于 P1-P6 六阶段推演的完整产出。*
*推演原始记录见 `冲刺/` 目录下的各阶段 txt 文件。*
*V1.0 MVP 的核心假设验证优先于任何工程完整性。*
